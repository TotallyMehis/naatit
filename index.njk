<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Naatit</title>
        <meta name="description" content="kun et muista miten ne heitetään...">
        <meta name="author" content="Miikka Ylätalo">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="site.css"> 
    </head>
    <body lang="fi">
        <div class="front">
            <div class="container">
                <div class="title-container">
                    <h1><a href="#">Naatit</a></h1>
                </div>
                <div class="flavor">
                    <p>kun et muista miten ne heitetään...</p>
                </div>
                <ul class="map-list">
                    {% for map in maps %}
                        <li>
                            <a href="/{{ map.name | slugify }}/">{{ map.name }}</a>
                            <ul>
                                {% for video in map.videos %}
                                    <li><a href="/{{ map.name | slugify }}/#{{ video.id }}">{{ video.title }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
</html>
